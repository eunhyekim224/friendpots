version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd api
      - npm install -g typescript
      - npm install
  build:
    commands:
      - tsc

artifacts:
  files:
    - api/package.json
    - api/package-lock.json
    - 'api/build/**/*'
